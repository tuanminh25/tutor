#include "stats.h"

auto read_marks(const std::string &path) -> std::vector<int> {
    (void)path;
    return {};
    // remove the above lines once implemented
}

auto calculate_stats(const std::vector<int> &marks) -> stats {
    (void)marks;
    // remove the above line once implemented

    return {};
}
