#include "permutation.h"

auto is_permutation(const std::string &x, const std::string &y) -> bool {
    // remove this line when properly implemented
    return static_cast<bool>(x.size()) && static_cast<bool>(y.size());
}
